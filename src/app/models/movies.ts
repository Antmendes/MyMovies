export interface Movies {
    items: [{
        id: string,
        title: string,
        fullTitle: string,
        year: string,
        releasestate: string,
        image: string,
        
    }]
}
